import React from 'react'
import RenderProps from './RenderProps'

const LikeImgProp = () => {
  return (
    <RenderProps>
        {
            ({count,handleCount}=()=>{
                
            })
        }
    </RenderProps>
  )
}

export default LikeImgProp